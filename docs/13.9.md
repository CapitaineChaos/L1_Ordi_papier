	L'exercice 13.9 existe toujours dans le nouveau cours et porte le numéro 10.7. Son énoncé est le suivant:

	Indiquer les mnémoniques qui correspondent aux valeurs suivantes dans la mémoire. Commenter le programme. Que fait-il ?

adresse  	valeur  	valeur 
50		49		70				IN α 70					(α) ← Entrée
52		40		70				LOAD α 70				A ← (α)
54		48		71				STORE α 71				(α) ← A
56		48		72				STORE α 72				(α) ← A
58		00		5E				LOAD # 5E				A ← V
5A		48		8D				STORE α 8D				(α) ← A
5C		10		74				JUMP α 74				PC ← α


5E		40		71				LOAD α 71				A ← (α)
60		48		72				STORE α 72				(α) ← A
62		40		70				LOAD α 70				A ← (α)
64		48		71				STORE α 71				(α) ← A
66		00		6C				LOAD # 6C				A ← V
68		48		8D				STORE α 8D				(α) ← A
6A		10		74				JUMP α 74				PC ← α
6C		41		71				OUT α 71				Sortie ← (α)
6E		10		6E				JUMP α 6E				PC ← α

70		00
71		00
72		00
73		00

74		00		00				LOAD # 00				A ← V
76		48		73				STORE α 73				(α) ← A
78		40		71				LOAD α 71				A ← (α)
7A		12		88				BRZ α 88				si A = 0 alors PC ← α

7C		21		01				SUB # 01				A ← A−V
7E		48		71				STORE α 71				(α) ← A
80		40		73				LOAD α 73				A ← (α)
82		60		72				ADD α 72				A ← A + (α)
84		48		73				STORE α 73				(α) ← A
86		10		78				JUMP α 78				PC ← α

88		40		73				LOAD α 73				A ← (α)
8a		48		71				STORE α 71				(α) ← A
8c		10		00				JUMP α 00				PC ← α




